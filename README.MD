# IO System Design

Install dependencies once from the repository root:

```sh
npm install
```

## 01-grpc

Run the gRPC inventory service and HTTP cart service:

```sh
npm run 01-grpc
```

To test it, open `01-grpc/cart-service.http` with the REST Client extension in VS Code/Cursor and click `Send Request` above each request.
