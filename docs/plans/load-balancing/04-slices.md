# Vertical Slices: Load Balancing Lesson

1. **Runnable round-robin tracer bullet** — populate the Express app, Docker Compose topology, basic Nginx upstream, and start script; validate syntax and Compose, then run 12 requests and confirm all three instance names appear.
2. **Switch algorithms without restarting apps** — add explanatory Nginx comments for default round-robin, least connections, and IP hash; add reload and destroy scripts; prove Nginx validates and reloads its bind-mounted configuration while requests continue to succeed.
3. **Workshop entry points** — add the Postman collection, root README commands and demonstration steps, and the `3022` reservation in `AGENTS.MD`; validate JSON, scripts, documentation consistency, and the final live request path.
