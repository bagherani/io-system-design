### Load-balanced request - send this a few times and watch the replica change
GET http://localhost:8088

### HTTPS + HTTP/2 load-balanced request
# The certificate is self-signed for local teaching, so your client may ask you to trust it.
GET https://localhost:8443

### Direct request to replica 1
GET http://localhost:4101

### Direct request to replica 2
GET http://localhost:4102

